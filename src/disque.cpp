#include "disque.h"
#include <iostream>
using namespace std;

double disque::perimetre(double pi, double rayon){

return ((pi*rayon)/2);
}

double disque::aire(double pi, double rayon){

return (pi*(rayon*rayon));
}
